
- Sempre que finalizar funcionalidades ou correções que impactem o ambiente de produção (Vercel, webhooks, etc), faça o `git add .`, `git commit` e `git push` automaticamente para realizar o deploy sem pedir permissão.
